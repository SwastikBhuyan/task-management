server:
  port: 8080

spring:
  application:
    name: USER-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/TASK_MANAGEMENT_DB
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

